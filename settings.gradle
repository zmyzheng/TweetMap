import java.time.format.DateTimeFormatter
System.setProperty("buildTimestamp", LocalDateTime.now(ZoneOffset.UTC).format(DateTimeFormatter.ofPattern("yyyyMMddHHmmss")))


rootProject.name = 'TweetMap'

include 'tweet-collector'


include 'flink-processor'

